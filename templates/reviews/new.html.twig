{% extends 'base.html.twig' %}

{% block title %}New Reviews{% endblock %}

{% block body %}
    <div class="container">
        <h1>Create new Reviews</h1>

        {{ include('reviews/_form.html.twig') }}

        <a href="{{ path('app_reviews_index') }}" class="btn btn-secondary mt-4">Back to list</a>
    </div>
{% endblock %}
