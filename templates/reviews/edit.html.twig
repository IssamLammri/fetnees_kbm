{% extends 'base.html.twig' %}

{% block title %}Edit Reviews{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h1 class="text-center">Edit Reviews</h1>

                <div class="my-4">
                    {{ include('reviews/_form.html.twig', {'button_label': 'Update'}) }}
                </div>

                <div class="text-center">
                    <a href="{{ path('app_reviews_index') }}" class="btn btn-secondary">Back to list</a>
                </div>

                <div class="my-4">
                    {{ include('reviews/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
