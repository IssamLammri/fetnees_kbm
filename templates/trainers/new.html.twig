{% extends 'base.html.twig' %}

{% block title %}New Trainers{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h1 class="text-center">Create new Trainers</h1>

                <div class="my-4">
                    {{ include('trainers/_form.html.twig') }}
                </div>

                <div class="text-center">
                    <a href="{{ path('app_trainers_index') }}" class="btn btn-secondary mt-3">Back to List</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
